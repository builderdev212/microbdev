name: black
on:
  push:
    paths:
      - '**/*.py'
  pull_request:
jobs:
  linter_name:
    name: Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: rickstaa/action-black@v1
        with:
          black_args: ". --check"